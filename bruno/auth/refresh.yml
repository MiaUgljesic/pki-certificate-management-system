info:
  name: refresh
  type: http

http:
  method: POST
  url: http://localhost:8080/api/auth/refresh
  body:
    type: json
    data: |-
      {
        "refreshToken": "0b0852d0-ea1d-4a6a-969b-29fb8e2d6ca4"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
