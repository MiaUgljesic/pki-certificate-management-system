info:
  name: download
  type: http

http:
  method: GET
  url: http:/localhost:8080/api/certificates/download
  headers:
    - name: Authorization
      value: "Bearer "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
